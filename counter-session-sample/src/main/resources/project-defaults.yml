swarm:
  logging:
    loggers:
      org.infinispan.topology:
        level: DEBUG
      org.jgroups.protocols:
        level: DEBUG
      org.xnio.safe-close:
        level: TRACE
    # root-logger:
    #   level: DEBUG
